Порождающие (Creational)
Singleton (Одиночка)
Один объект на весь runtime. Глобальная точка доступа.
Пример: конфигурация приложения.

Factory Method (Фабричный метод)
Интерфейс для создания объектов, но выбор подкласса — в подклассе.
Пример: создание разных типов уведомлений.

Abstract Factory (Абстрактная фабрика)
Создание семейств объектов без указания конкретных классов.
Пример: GUI-кнопки для разных ОС.

Builder (Строитель)
Пошаговая сборка сложного объекта. Отделение создания от представления.
Пример: конструирование HTML-документа.

Prototype (Прототип)
Клонирование объекта без привязки к классу.
Пример: копирование фигуры в редакторе.

🧱 Структурные (Structural)
Adapter (Адаптер)
Приведение несовместимых интерфейсов к общему виду.
Пример: обёртка старого API.

Bridge (Мост)
Разделение абстракции и реализации для независимого изменения.
Пример: разные формы и стили отображения.

Composite (Компоновщик)
Иерархия из объектов, где клиент работает одинаково с одним и многими.
Пример: дерево UI-компонентов.

Decorator (Декоратор)
Динамическое расширение поведения объекта без изменения его класса.
Пример: обёртка логирования.

Facade (Фасад)
Унифицированный интерфейс к подсистеме. Скрывает сложность.
Пример: простой API поверх сложной библиотеки.

Flyweight (Приспособленец)
Разделение общего состояния между множеством объектов.
Пример: кэш одинаковых символов.

Proxy (Заместитель)
Контролирует доступ к другому объекту (ленивый, защищённый, сетевой и т. д.).
Пример: удалённый сервис.

⚙️ Поведенческие (Behavioral)
Chain of Responsibility (Цепочка обязанностей)
Передача запроса по цепочке обработчиков.
Пример: middleware в веб-сервере.

Command (Команда)
Объект-запрос. Разделяет отправителя и получателя.
Пример: история операций undo/redo.

Interpreter (Интерпретатор)
Интерпретация грамматики для языка.
Пример: обработка выражений или фильтров.

Iterator (Итератор)
Унифицированный доступ к элементам коллекции.
Пример: for-in в Python.

Mediator (Посредник)
Объект-контроллер взаимодействия других объектов.
Пример: центр обмена событиями.

Memento (Снимок)
Сохранение и восстановление состояния без раскрытия деталей.
Пример: состояние игры.

Observer (Наблюдатель)
Подписчики автоматически уведомляются об изменениях.
Пример: UI подписан на модель.

State (Состояние)
Поведение объекта зависит от текущего состояния.
Пример: автомат с напитками.

Strategy (Стратегия)
Взаимозаменяемые алгоритмы с одинаковым интерфейсом.
Пример: сортировка с разными методами.

Template Method (Шаблонный метод)
Общая структура алгоритма с переопределяемыми шагами.
Пример: этапы рендера.

Visitor (Посетитель)
Добавление операций к объектам без изменения их классов.
Пример: обход AST.